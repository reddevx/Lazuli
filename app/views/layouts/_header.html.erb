<nav class="navbar navbar-expand-lg bg-dark text-white container p-3">
  <div class="container-fluid">

     <%= link_to 'Blog', root_path, class: 'text-decoration-none p-1' %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

  <div class="collapse navbar-collapse" id="expanded-menu">
    <ul class="nav navbar-nav">
      <% if !user_signed_in? %>
        <li class="active">
          <%= link_to 'Nova Conta', cadastro_path, class: 'p-1' %>
        </li>
      <% end %>
    </ul>

    
      <% if !user_signed_in? %>
        <li>
          <%= link_to 'Entrar', entrar_path, class:'p-1' %>
      </li>
      <% else %>
        <p class="p-2">bem vindo, <%= current_user.name %></p>
           
            <li>
              <%= link_to 'Minha Pagina', user_path(current_user), class: 'p-1' %>
            </li>
            <li>
              <%= link_to 'Editar', '#' %>
            </li>
            <li class="divider" role="separator"></li>
            <li>
              <%= link_to 'Sair', sair_path, method: :delete, class:'text-decoration-none p-1' %>
            </li>
          </ul>
        </li>
      <% end %>

    </div>
  </div>
</nav>