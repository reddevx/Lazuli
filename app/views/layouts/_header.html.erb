<nav class="navbar navbar-expand-lg bg-body-secondary text-white p-3">
  <div class="container-fluid">
     <%= link_to 'Lazuli.', root_path, class: 'text-decoration-none text-primary fw-bold h4' %>
     
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
 <%= link_to 'Sobre', sobre_path, class: 'text-decoration-none text-primary p-2' %>
 <%= link_to 'Contato', contato_path, class: 'text-decoration-none text-primary p-2' %>
 <%= link_to 'Termos', terms_path, class: 'text-decoration-none text-primary p-2' %>


  <div class="collapse navbar-collapse" id="expanded-menu">
    <ul class="nav navbar-nav">
      <% if !user_signed_in? %>
        <li class="active" style="position: absolute; top: 1; right: -0;">
          <%= link_to 'Nova Conta', cadastro_path, class: ' btn btn-primary p-1' %>
          <%= link_to 'Entrar', entrar_path, class: ' btn btn-primary p-1' %>s
        </li>
    
      <% end %>
    </ul>

    
      <% if !user_signed_in? %>
      
      <% else %>
      <br>


<div class="dropdown p-1" style="position: absolute; top: 1; right: 0;">
<a class="btn btn-primary dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
  <span style="display: flex; align-items: center;">
    <%= image_tag(current_user.image.url, style: 'width: 20px; height: 20px; border-radius: 50%; margin-right: 5px;') %>
    <%= current_user.na
    e %>
  </span>
</a>

  <ul class="dropdown-menu bg-primary text-white">
    <li>
    <%= link_to 'Publicações', posts_path(current_user), class:'text-decoration-none p-1 text-light'%>
    </li>

    <li><%= link_to 'Meu Perfil', user_path(current_user), class:'text-decoration-none p-1 text-light' %>
    </li>
    <li>
    <%= link_to 'Editar', edit_user_path(current_user), class:'text-decoration-none p-1 text-light'%>
    </li>
    <li><a class="dropdown-item" href="#">
   <%= link_to 'Sair', sair_path, method: :delete, class:'text-decoration-none p-1 text-light' %>
    </li>
  </ul>
</div>

      
      <% end %>

    </div>
  </div>
</nav>


