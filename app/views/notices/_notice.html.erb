<div id="<%= dom_id notice %>">
  <p>
    <strong>Title:</strong>
    <%= notice.title %>
  </p>

  <p>
    <strong>Content:</strong>
    <%= notice.content %>
  </p>

<p>
  <%= image_tag(notice.image.url) if notice.image.present? %>
</p>

</div>
